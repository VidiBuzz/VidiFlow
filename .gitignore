.vercel
node_modules

# ===== SECRETS — NEVER PUSH THESE =====
.gdrive-credentials/
*.json
!package.json
!package-lock.json
!railway.json
!*.config.json

# Secret-containing session files
session-*.md

# Google OAuth / API credentials
gcp-oauth.keys.json
client_secret_*.json
credentials.json

# Environment files with secrets
.env
.env.*
*.key
*.pem
